package de.lyca.xslt.conferr;

import static de.lyca.xslt.ResourceUtils.getSource;

import javax.xml.transform.Source;

import org.custommonkey.xmlunit.Transform;
import org.junit.Ignore;
import org.junit.Test;

public class ConfNamedTemplateErrTests {

  private static final String PACKAGE = '/' + ConfNamedTemplateErrTests.class.getPackage().getName().replace('.', '/')
          + "/namedtemplateerr/";

  @Test
  @Ignore
  public void namedtemplateerr01() throws Exception {
    final String name = PACKAGE + "namedtemplateerr01";
    final Source xsl = getSource(name + ".xsl");
    final Source xml = getSource(name + ".xml");
    final Transform t = new Transform(xml, xsl);
  }

  @Test
  @Ignore
  public void namedtemplateerr02() throws Exception {
    final String name = PACKAGE + "namedtemplateerr02";
    final Source xsl = getSource(name + ".xsl");
    final Source xml = getSource(name + ".xml");
    final Transform t = new Transform(xml, xsl);
  }

  @Test
  @Ignore
  public void namedtemplateerr03() throws Exception {
    final String name = PACKAGE + "namedtemplateerr03";
    final Source xsl = getSource(name + ".xsl");
    final Source xml = getSource(name + ".xml");
    final Transform t = new Transform(xml, xsl);
  }

  @Test
  @Ignore
  public void namedtemplateerr04() throws Exception {
    final String name = PACKAGE + "namedtemplateerr04";
    final Source xsl = getSource(name + ".xsl");
    final Source xml = getSource(name + ".xml");
    final Transform t = new Transform(xml, xsl);
  }

  @Test
  @Ignore
  public void namedtemplateerr05() throws Exception {
    final String name = PACKAGE + "namedtemplateerr05";
    final Source xsl = getSource(name + ".xsl");
    final Source xml = getSource(name + ".xml");
    final Transform t = new Transform(xml, xsl);
  }

  @Test
  @Ignore
  public void namedtemplateerr06() throws Exception {
    final String name = PACKAGE + "namedtemplateerr06";
    final Source xsl = getSource(name + ".xsl");
    final Source xml = getSource(name + ".xml");
    final Transform t = new Transform(xml, xsl);
  }

  @Test
  @Ignore
  public void namedtemplateerr07() throws Exception {
    final String name = PACKAGE + "namedtemplateerr07";
    final Source xsl = getSource(name + ".xsl");
    final Source xml = getSource(name + ".xml");
    final Transform t = new Transform(xml, xsl);
  }

  @Test
  @Ignore
  public void namedtemplateerr08() throws Exception {
    final String name = PACKAGE + "namedtemplateerr08";
    final Source xsl = getSource(name + ".xsl");
    final Source xml = getSource(name + ".xml");
    final Transform t = new Transform(xml, xsl);
  }

  @Test
  @Ignore
  public void namedtemplateerr09() throws Exception {
    final String name = PACKAGE + "namedtemplateerr09";
    final Source xsl = getSource(name + ".xsl");
    final Source xml = getSource(name + ".xml");
    final Transform t = new Transform(xml, xsl);
  }

  @Test
  @Ignore
  public void namedtemplateerr10() throws Exception {
    final String name = PACKAGE + "namedtemplateerr10";
    final Source xsl = getSource(name + ".xsl");
    final Source xml = getSource(name + ".xml");
    final Transform t = new Transform(xml, xsl);
  }

  @Test
  @Ignore
  public void namedtemplateerr11() throws Exception {
    final String name = PACKAGE + "namedtemplateerr11";
    final Source xsl = getSource(name + ".xsl");
    final Source xml = getSource(name + ".xml");
    final Transform t = new Transform(xml, xsl);
  }

  @Test
  @Ignore
  public void namedtemplateerr12() throws Exception {
    final String name = PACKAGE + "namedtemplateerr12";
    final Source xsl = getSource(name + ".xsl");
    final Source xml = getSource(name + ".xml");
    final Transform t = new Transform(xml, xsl);
  }

  @Test
  @Ignore
  public void namedtemplateerr13() throws Exception {
    final String name = PACKAGE + "namedtemplateerr13";
    final Source xsl = getSource(name + ".xsl");
    final Source xml = getSource(name + ".xml");
    final Transform t = new Transform(xml, xsl);
  }

  @Test
  @Ignore
  public void namedtemplateerr14() throws Exception {
    final String name = PACKAGE + "namedtemplateerr14";
    final Source xsl = getSource(name + ".xsl");
    final Source xml = getSource(name + ".xml");
    final Transform t = new Transform(xml, xsl);
  }

  @Test
  @Ignore
  public void namedtemplateerr15() throws Exception {
    final String name = PACKAGE + "namedtemplateerr15";
    final Source xsl = getSource(name + ".xsl");
    final Source xml = getSource(name + ".xml");
    final Transform t = new Transform(xml, xsl);
  }

  @Test
  @Ignore
  public void namedtemplateerr16() throws Exception {
    final String name = PACKAGE + "namedtemplateerr16";
    final Source xsl = getSource(name + ".xsl");
    final Source xml = getSource(name + ".xml");
    final Transform t = new Transform(xml, xsl);
  }

}
